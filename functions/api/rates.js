export async function onRequestGet(c){const{env}=c;try{const kv=env.RATES_KV;if(kv){const b=await kv.get('rates.json');if(b)return new Response(b,{headers:{'content-type':'application/json'}})}}catch(e){}const u=new URL('/rates.json',c.request.url);const r=await fetch(u.toString(),{cf:{cacheTtl:0}});return new Response(await r.text(),{headers:{'content-type':'application/json'}})}