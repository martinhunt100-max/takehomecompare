export async function onRequestPost(c){const{env}=c,{request}=c;const b=await request.json().catch(()=>({}));const p=b.plan||'monthly';const S=env.STRIPE_SECRET_KEY;if(!S)return new Response(JSON.stringify({error:'STRIPE_SECRET_KEY missing'}),{status:500});const pm=env.PRICE_ID||env.PRICE_ID_MONTHLY;const pa=env.PRICE_ID_ANNUAL||pm;const price=p==='annual'?pa:pm;const su=env.SUCCESS_URL||'https://takehomecompare.com/advanced-v2.html?pro=1';const cu=env.CANCEL_URL||'https://takehomecompare.com/advanced-v2.html';const d=new URLSearchParams();d.append('mode','subscription');d.append('automatic_tax[enabled]','true');d.append('success_url',su);d.append('cancel_url',cu);d.append('line_items[0][price]',price);d.append('line_items[0][quantity]','1');const resp=await fetch('https://api.stripe.com/v1/checkout/sessions',{method:'POST',headers:{authorization:`Bearer ${S}`,'content-type':'application/x-www-form-urlencoded'},body:d});if(!resp.ok)return new Response(JSON.stringify({error:'Stripe error'}),{status:500});const j=await resp.json();return new Response(JSON.stringify({url:j.url}),{headers:{'content-type':'application/json'}})}