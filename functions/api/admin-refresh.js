export async function onRequestPost(c){const{request,env}=c;const t=(request.headers.get('authorization')||'').replace(/^Bearer\s+/i,'');if(!env.ADMIN_TOKEN||t!==env.ADMIN_TOKEN)return new Response('Unauthorized',{status:401});const kv=env.RATES_KV;const prev=await kv.get('rates.json');const base=prev?JSON.parse(prev):{version:'v0.5-prod',US:{}};base.updated=new Date().toISOString();await kv.put('rates.json',JSON.stringify(base));return new Response(JSON.stringify({ok:true,updated:base.updated}),{headers:{'content-type':'application/json'}})}